Pillow>=10.0.0
rembg[cpu,cli]>=2.0.0
opencv-contrib-python-headless>=4.9.0
playwright>=1.45.0
jinja2>=3.1.0
feedparser>=6.0.0
gspread>=6.0.0
oauth2client>=4.1.3
requests>=2.31.0
pyyaml>=6.0

# Optional - stealth article fetching (og:image + body text past 403/hotlink
# blocks) in scripts/fetchers.py. Everything degrades to a plain urllib GET
# if this isn't installed, so it's not a hard requirement for unattended runs.
# The [fetchers] extra pulls in curl_cffi + patchright/playwright browser
# binaries - after installing, run: playwright install chromium
scrapling[fetchers]>=0.2.9
